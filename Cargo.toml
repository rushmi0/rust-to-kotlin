[package]
name = "rustlin"
version = "0.1.0"
edition = "2024"

[lib]
name = "rustlin"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
uniffi = { version = "0.32.0", features = ["tokio", "cli"] }