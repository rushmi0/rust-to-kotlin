[package]
name = "demo"
version = "0.1.0"
edition = "2024"

[lib]
name = "demo"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
uniffi = { version = "=0.29.4", features = ["tokio", "cli"] }